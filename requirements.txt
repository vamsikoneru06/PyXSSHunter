
## 📖 Usage

### Basic Scan

**Using Docker:**
```powershell
# Windows PowerShell
docker run --rm `
  -v "${PWD}/reports:/app/reports" `
  pyxsshunter:latest `
  scan `
    --url "http://testphp.vulnweb.com/search.php?test=1" `
    --stealth-level medium `
    --max-payloads 80 `
    --output /app/reports